// T8.4 — Kabinler page now renders the NOC design overview (mockup-faithful
// rack card grid + 6-stat row + U-elevation detail with Cihazlar/Items tabs,
// real read+write data). The previous full management implementation
// (drag-drop placement, SwitchPortPanel, Leaflet map view, ...) is preserved
// in git history; its actions will be re-wired in the real-data pass.
export { default } from './NocRacks'
