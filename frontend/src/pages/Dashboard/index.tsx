// T8.4 — Dashboard now renders the NOC design dashboard (mockup-faithful,
// nm-card / nm-grid widget system on real data). The previous TV-style
// implementation is preserved in git history.
export { default } from './NocDashboard'
