// T8.4 — Services page now renders the NOC design Servisler surface
// (mockup-faithful card grid + 6-stat row + detail drawer, real servicesApi
// + per-service impact). The previous antd Table implementation is preserved
// in git history; CRUD/Transfer/impact flows are kept identical, just
// re-styled.
export { default } from './NocServices'
