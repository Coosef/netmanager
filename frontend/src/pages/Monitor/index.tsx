// T8.4 — Monitor page now renders the NOC design Uyarılar surface
// (mockup-faithful incident-style event cards + 6-stat row, real data via
// monitorApi). The previous timeline/table implementation is preserved in
// git history; the same actions (scan, ack, ack-all, purge, CSV) are kept.
export { default } from './NocMonitor'
