// T8.4 — Agents page now renders the NOC design overview (mockup-faithful
// agent card grid + 6-stat row, real read data). The previous full management
// implementation (create/delete/detail drawer/security/command logs) is
// preserved in git history; its actions get re-wired in the real-data pass.
export { default } from './NocAgents'
