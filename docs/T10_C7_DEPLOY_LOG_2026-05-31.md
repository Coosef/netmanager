# T10 C7 Production Deploy Log — 2026-05-31

> **STATUS: PRODUCTION DEPLOY COMPLETED ✅**
> P1–P7 yeşil · P8 backend yeşil · P8 browser SW/cache teşhisi + cosmetic bugfix
> sonrası tarayıcı GREEN · Cloudflare `/poe` banner anomalisi kapsam-dışı follow-up'a alındı.
> Bu doküman geri-dönüş anchor'ı + post-mortem.

## Özet

- **Hedef:** VPS prod (`93.180.133.88`, `/opt/netmanager`)
  `b3d596b` / `f9adsecrls` → `ff35bc2` / `f9aeportpol` → **`d1f8e6f`** (post-bugfix final).
- **Kapsam:** T10 **C7 epiği** (Device Detail Page + per-port policy override):
  - **C7.A** backend — `port_policy_assignments` tablo (f9ae migration), `/devices/{id}/port-policy-assignments` GET/POST(bulk)/PATCH/DELETE endpoint, resolver chain (override → cihaz default → org default → fallback). Merge `107fc37`.
  - **C7.B** frontend scaffold — `/devices/:deviceId` route + `DeviceDetailPage` sekme katalogu (9 sekme), Overview + Security tab canlı, eski `DeviceDetail` modal deprecate. Merge `d89b192`.
  - **C7.C** Portlar sekmesi — tablo (interface federasyonu + effective policy kolonu + kaynak Tag'i + MAC count + dry-run flap ⚠), sticky bulk toolbar, `BulkPolicyAssignDrawer`, override-kaldır mutation, Shutdown disabled placeholder. Merge `63784b4`.
  - **C7.D** kalan 6 sekme — VLAN / MAC / PoE / Olaylar / Backup / Aksiyonlar; `DevicePortsPage` silindi + `?tab=ports` redirect; tüm placeholder bayraklar kaldırıldı; 3 smoke test eklendi. Merge `ff35bc2`.
  - **Post-bugfix** — Security tab'da kalmış "C7.C ile gelecek" Alert temizliği. Branch `t10/c7-post-bugfix-1` → merge `d1f8e6f`.
- **DB:** 1 additive migration (`f9ae_port_policy_assignments`) — `CREATE TABLE` + 3 index + RLS FORCE + `ppa_org_isolation` policy (Faz 7 pattern) + `netmgr_app` GRANT. **Forward-destructive yok.**
- **Compose/network/env:** Değişiklik **YOK** — B1c segmentation aynen korundu (host yalnız 80+443).
- **Frontend:** production stage. C6b Drawer Güvenlik Politikası kaldırıldı, **Detail Page Security sekmesine taşındı**. Bundle hash `index-W_5L7ooN.js` (C7 deploy) → `index-RNNEsWOM.js` (post-bugfix).
- **Kesinti:** P7 recreate ~33sn blip (backend+frontend); bugfix recreate yalnız frontend, <1sn blip. Rollback yok, restart loop yok.
- **C5 (gerçek auto-quarantine):** kapsam-dışı bırakıldı — `ActionsTab` + `PortsTab`'da Shutdown DISABLED + tooltip "C5 ile gelecek" placeholder'ı.

## Final state (POST-DEPLOY ANCHOR, post-bugfix)

| | Değer |
|---|---|
| git HEAD | **`d1f8e6f6bd8aa747c8cba8c2b489a61ec08c496d`** (d1f8e6f, main) |
| alembic current | **`f9aeportpol`** |
| 11/11 servis | Up/healthy (frontend 12dk, diğerleri 10sa/2gün) |
| Host listening | yalnız `0.0.0.0:80` + `0.0.0.0:443` (B1c korundu) |
| Disk | 47G / 33G kullanılan / 12G boş / %74 (deploy öncesiyle aynı) |
| RLS-forced tablo | **61** (60 → 61, +1 `port_policy_assignments`) |
| RLS policy sayısı | 63 (+1 `ppa_org_isolation`) |
| DB perm audit | **25/25 PASS → GO** |

### Çalışan image digest'leri (final)

```
netmanager-frontend-1               ee492c9bb0f9d25a0934ab91bc18a324dedbdedf7c127cae0bb3af10f62296e4  (bugfix build)
netmanager-backend-1                88b7480009894f9637d26f244c61806f97ce967d4d07c8ae9f6dd26e273cdcb7  (C7 build)
netmanager-celery_default_worker-1  7b860886da0c207f9540a9e4143b42f82ef454cf9512d5bf99dce3f97962581a
netmanager-celery_worker-1          09ef1ae6fec99f547d9a469a8d405d01fb595048a616087cfb5b074ce030c1f3
netmanager-celery_agent_worker-1    ec9c54caf49367302e634f417fe6f89b96ab15272f21df961fa0488dc1086a0b
netmanager-celery_beat-1            c7e6e92c3ac9fac6aff67dcc40cb8d8b38506b22cc6dcce59c4c98d9ac9dce6b
netmanager-event_consumer-1         d1034c2940ce9c36b68ce791e08c202824f5e111b3a8ad465126832c333af1a7
netmanager-flower-1                 e5616f1b31b8c13a24ad798e432a6140fb1950abb67af6678fcd0c9b6b7613e5
netmanager-nginx-1                  6769dc3a703c719c1d2756bda113659be28ae16cf0da58dd5fd823d6b9a050ea  (T10 deploy'dan beri)
netmanager-postgres-1               fffeb6af732486273a29559a4caefe1b917f0324703c6e1356e2fac3ba267f9d  (T10 deploy'dan beri)
netmanager-redis-1                  13b871aaf97fdce1787aef62e24345c07906bdca4f38e947550286d917b737ee  (T10 deploy'dan beri)
```

## PRE-DEPLOY ROLLBACK ANCHOR (geri-dönüş için saklı)

| | Değer |
|---|---|
| git | `b3d596b00cd44d8496451a269f5c0b0c1a2df467` (T10 deploy son hali) |
| alembic | `f9adsecrls` |
| pg_dump | `/opt/netmanager/backups/pre-c7-deploy-20260530_112911.dump` (12M, custom format) |
| dump SHA-256 | `6c283d13d9a6bd3039a7ba91d5b5bca61ab138b4b695305007249364b6eca322` |
| sidecar SHA dosyası | `backups/pre-c7-deploy-20260530_112911.sha256` |
| pre-deploy frontend image | `sha256:9120d0ba…` (C7 build, RNNEsWOM öncesi `W_5L7ooN`) |
| pre-deploy backend image | `sha256:88b7480…` (C7 build, halen canlı — bugfix backend'i değiştirmedi) |

> Rollback prosedürü: `docs/DR_RUNBOOK.md` §6. Tek başına `alembic downgrade f9adsecrls` da yeterli olabilir (f9ae additive; drop_table güvenli, override veri yoktu deploy esnasında — bugfix sonrası kullanıcı testleriyle 3 override yazıldı, downgrade öncesi bunun gözden geçirilmesi gerekir).

## P-fazı icra özeti (C7 ana deploy)

| P | Adım | Yazma | Sonuç |
|---|---|---|---|
| P1 | `pg_dump -Fc` + SHA-256 sidecar | dosya | ✅ 12M dump, SHA yazıldı |
| P2 | rollback anchor (git+alembic+11 image) | yok | ✅ Read-only, anchor kaydedildi |
| P3 | disk + ingress + network check | yok | ✅ 12G boş; postgres+backend `netmanager_internal`'da; Cloudflared localhost:80 (panel teyit) |
| P4 | `git fetch` + `git merge --ff-only ff35bc2` | working tree | ✅ HEAD = `ff35bc2`; tracked clean |
| P5 | `docker compose build` | image | ✅ backend+frontend yeni digest; çalışan eski image'da kalır |
| P6 | `docker compose run --rm -T backend alembic upgrade head` | DB (CREATE TABLE+index+RLS+GRANT) | ✅ `f9adsecrls → f9aeportpol`; B1c sonrası compose run sorunsuz çalıştı (T10'da B1c-ağ fallback'ı gerekmişti, C7'de gerekmedi) |
| P7 | `docker compose up -d` | servis swap | ✅ 8 servis recreate (postgres/redis/nginx dokunulmadı, uptime 38sa+ korundu); ~33sn blip |

## P8 — Smoke gates

### Backend (creds-less + RLS + DB perm) — GREEN ✅
```
/health/ready             200
/login                    200  (SPA)
/devices/1                200  (SPA)
/devices/1?tab=security   200
/devices/1?tab=ports      200
/api/v1/devices/1/port-policy-assignments (unauth)  401
backend log son 2dk       temiz (5xx/traceback/oauth2 yok)
```

RLS izolasyon (port_policy_assignments):
- org1 cross-org leak = **0** ✅
- super-admin total = 0 (deploy anında henüz override yazılmamıştı)
- alembic_version = `f9aeportpol`
- RLS-forced tablo sayısı: 60 → **61** ✅

DB perm audit: **25/25 PASS → GO** (netmgr_app NOSUPERUSER/NOBYPASSRLS/NOCREATEDB/NOCREATEROLE; CREATE TABLE/SCHEMA DENIED; public tablo sahipliği=0; RLS-enabled tablo 61; RLS policy 63).

### Browser — SW/cache teşhisi → cosmetic bugfix → GREEN ✅

**İlk gözlem (RED):** kullanıcı `/devices` tıkladığında **eski modal** açıldı, yeni Detail Page çıkmadı.

**Teşhis:** yerel kod tarafı temiz (`Devices/index.tsx`'te modal çağrı yeri yok, tüm `onDetail` → `navigate('/devices/:id')`). Prod'daki container **YENİ** image (`9120d0ba`), bundle **YENİ** (`index-W_5L7ooN.js` içinde `port-policy-assignments` API path string match'i). Service Worker config'i (`sw.js`) `skipWaiting + clientsClaim + cleanupOutdatedCaches` ayarlı, `precache` yalnız 3 ikon/manifest (JS bundle precache edilmiyor). Sonuç: **tarayıcı bellekteki eski JS modülleri**. Hard reload (Cmd+Shift+R) ile çözüldü.

**İkinci tur (kısmen RED):** Security dropdown'ları "boş" göründü; Ports bulk toolbar görünmüyor; Security tab'da eski "C7.C ile gelecek" Alert duruyor.

**Teşhis:**
- **Dropdown'lar:** backend log'da `/security-policies/port` 200 + 77ms (kayıt var); test sonrası `Cmd+Shift+R` ile yeniden açıldığında dropdown'lar **GREEN** çalıştı (Switch + Port options listesi geldi, policy ataması kaydedildi, resolver chain doğru güncellendi: Switch atanan→Default, Portlar cihaz default→ap).
- **Bulk toolbar:** `{canWrite && selected.length > 0 && (...)}` guard — checkbox seçilmeden render olmaz (bilinçli tasarım). Kullanıcı 3 port seçti → toolbar göründü → "kamera" policy 3 porta atandı → "3 port güncellendi" mesajı → port listesinde "kamera/override" Tag'leri persist. **GREEN**.
- **Eski Alert:** gerçek cosmetic bug — C7.C tamamlandığı halde [`SecurityPoliciesTab.tsx:173-178`](../frontend/src/pages/Devices/detail/SecurityPoliciesTab.tsx)'deki Alert güncellenmedi.

## Bugfix mini-deploy (`t10/c7-post-bugfix-1`)

| M | Adım | Sonuç |
|---|---|---|
| M1 | `git merge --no-ff t10/c7-post-bugfix-1` → main | ✅ merge commit `d1f8e6f` |
| M2 | `git push origin main` | ✅ `92ab220..d1f8e6f` |
| M3 | prod `git fetch` + `git merge --ff-only d1f8e6f` | ✅ tek dosya 3+/10- update |
| M4 | `docker compose build frontend` (yalnız frontend) | ✅ image `9120d0ba` → `ee492c9b` |
| M5 | `docker compose up -d frontend` | ✅ frontend recreate <1sn; backend/celery/postgres/redis/nginx dokunulmadı (uptime stabil) |
| M6 | Post-deploy smoke | ✅ Bundle hash `W_5L7ooN.js` → `RNNEsWOM.js`; SPA routes 200; tüm 11 container healthy |
| M7 | Tarayıcı browser teyit (kullanıcı) | ✅ Alert yok, dropdown'lar çalışıyor, 3 override Tag'leri persist |

**Bugfix kapsamı:** 1 dosya (`SecurityPoliciesTab.tsx`), 3 insert / 10 delete. Alert + import + 2 yorum referansı. tsc clean, vitest 189/189, vite build OK.

## Doğrulama matrisi

| Sekme | Render | Veri | Yetki | Doğrulayan |
|---|---|---|---|---|
| Genel | ✅ | cihaz meta + agent + lifecycle | viewer+ | M7 |
| Portlar | ✅ | interface + effective policy + kaynak Tag + dry-run pill | viewer read / org_admin+ yazma | M7 |
| Güvenlik Politikası | ✅ | Switch + Port dropdown + Effective Resolver Zinciri | viewer read / org_admin+ yazma | M7 |
| VLAN | ✅ | gerçek VLAN tablosu | viewer+ | P8 browser |
| MAC | ✅ | mac-arp federation + arama | viewer+ | P8 browser |
| PoE | ✅ | snapshot (boş → friendly empty, beklenen) | viewer+ | P8 browser |
| Olaylar | ✅ | severity + policy_only chip + Drawer | viewer+ | P8 browser |
| Backup | ✅ | tarihçe + indir | viewer read / org_admin+ yazma | P8 browser |
| Aksiyonlar | ✅ | 4 kart + Shutdown DISABLED + Sil Popconfirm hostname/IP | role-gated | P8 browser |

## Riskler & kabul edilenler

- **Cloudflare `/poe` banner** — P7 GREEN sonrası `/poe` sayfasında "The origin web server returned an invalid or incomplete response to Cloudflare" banner'ı görüldü. Sayfa fonksiyonel olarak açıldı (0 W stats + "Henüz PoE snapshot kaydı yok"). Backend log temiz, /health 200, creds-less gate'ler temiz. **Kapsam-dışı** olarak kabul edildi; aşağıdaki follow-up'a alındı.
- **Bundle büyüklüğü** (>500KB chunk uyarısı) — vite build OK, pre-existing.
- **PoeTab `retry:false` 404 toleransı** — friendly empty kullanıcı testinde teyit edildi.
- **`DeviceDetail.tsx` dosyası diskte duruyor** ama hiçbir yerden import edilmiyor (dead code). C7.D notunda "embed referans için korundu" deniyordu; pratikte kullanılmadı. v2.1'de silinmeli.
- **`DevicePortsPage` silindi** — `/devices/:id/ports` URL'i C7.B'de `?tab=ports` redirect'e bağlandı; eski bookmark'lar kırılmıyor.

## Follow-up listesi

| # | Konu | Öncelik | Notlar |
|---|---|---|---|
| 1 | **Cloudflare `/poe` banner soruşturması** | Yüksek | Sebebi tespit edilmedi; fonksiyonel etkisi yok ama edge cache / CF Worker / origin timeout incelek gerek. Tetik: `/poe` (global PoE Dashboard) sayfasında. Detay analiz için CF dashboard + edge curl + backend trace gerek. |
| 2 | `DeviceDetail.tsx` modal dosyası silinmesi | Orta | Dead code; v2.1 cleanup PR'ında. |
| 3 | Vendor-alias port_name normalization | Orta | C7.A backend planında v2.1 olarak listelendi. |
| 4 | C5 gerçek auto-quarantine | Orta-Yüksek | Approval + kill-switch tasarımı + Shutdown butonlarının enable edilmesi. Placeholder hazır. |
| 5 | React Query queryKey'lerine `organizationId` eklenmesi | Düşük | Faz 7 plan'ı Phase 6c önerisi; org switch'te cache leak'i önlemek için defensive. Bu deploy'da gerekmedi. |

## Commit kronolojisi (kapsam: C7 epiği + bugfix)

```
d1f8e6f  Merge branch 't10/c7-post-bugfix-1' — Security tab yanıltıcı C7.C placeholder alert kaldır
664b717  fix(c7-bugfix-1): SecurityPoliciesTab — yanıltıcı C7.C placeholder alert kaldır
92ab220  docs(c7): production deploy mini-zincir planı
ff35bc2  Merge branch 't10/c7d-remaining-tabs' — C7.D Device Detail kalan 6 sekme + cleanup
d0d5bca  feat(c7d): ActionsTab + DevicePortsPage sil + tüm placeholder bayraklar kaldır + smoke
7d97276  feat(c7d): BackupTab — getBackups + indir/altın işaretle + takeBackup
02874df  feat(c7d): EventsTab — monitorApi.getEvents device-filter + severity/policy_only chip + Drawer detay
…       (C7.D commit serisi devamı — PoeTab, MacTab, VlanTab)
63784b4  Merge branch 't10/c7c-ports-tab' — Portlar sekmesi + bulk drawer + override kaldır
d89b192  Merge branch 't10/c7b-detail-page' — DeviceDetailPage scaffold + Security tab
107fc37  Merge branch 't10/c7a-port-policy-assignments' — backend tablo + endpoint + resolver
```

## Rollback prosedürü (geri dönüş gerekirse)

**Tetikleyiciler:** SPA route 5xx (P8 sonrası) · RLS izolasyon kırıldı · DB audit FAIL · backend 5xx sürekli artıyor · kullanıcılar Detail Page'e ulaşamıyor.

**Prosedür:**
1. Yazan servisleri durdur: `docker compose stop backend celery_worker celery_default_worker celery_agent_worker celery_beat event_consumer flower`
2. P1 dump restore: `cat backups/pre-c7-deploy-20260530_112911.dump | docker exec -i netmanager-postgres-1 pg_restore -U netmgr -d network_manager --clean --if-exists`
3. Kod rollback: `git checkout b3d596b` veya `git reset --hard b3d596b`
4. Image rollback: `docker tag <pre-deploy frontend digest> netmanager-frontend:latest` (pre-deploy: `9120d0ba` C7 hali — tam pre-C7 için T10 deploy log'undan `f66fd740…` çekilir); aynısı backend için (`88b7480…` C7 hali — tam pre-C7 için `d8b2a79e…`)
5. `docker compose up -d`
6. P8 gate 1-3 yeşil + `alembic current = f9adsecrls` teyit

**Tek başına `alembic downgrade f9adsecrls`** — f9ae additive olduğu için drop_table güvenli; ancak post-deploy kullanıcı override yazdığı için (3 port `kamera`), önce o veriyi gözden geçir veya dump restore tercih et.

---

## P9 Status

**DEPLOY COMPLETED ✅** — C7 epiği canlıda fonksiyonel olarak çalışıyor; cosmetic temizlik yapıldı; tüm 11 container healthy; geri dönüş anchor'ı + dump + image digest'leri kayıtlı.

Sıradaki çalışma: Cloudflare `/poe` banner soruşturması (follow-up #1) — ayrı bir görev olarak ele alınacak.
