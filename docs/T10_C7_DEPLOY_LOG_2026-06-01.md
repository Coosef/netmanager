# T10 C7 Dalga 1 Production Deploy Log — 2026-06-01

> **STATUS: PRODUCTION DEPLOY COMPLETED ✅**
> Tüm 5 P-fazı yeşil; canlı doğrulama temiz. Wave 1 (Feature Restore) merge edildi.
> Backend genişletme dahil ilk Wave 1 deploy. Bu doküman geri-dönüş anchor'ı + post-mortem.

## Özet

- **Hedef:** VPS prod (`93.180.133.88`, `/opt/netmanager`)
  `d1f8e6f` / `f9aeportpol` → **`f23ca83`** (Wave 1 merge — `f9aeportpol` korundu, DB migration YOK).
- **Kapsam:** T10 **C7 Dalga 1** — modal→sayfa geçişindeki 7 kritik özellik restore + 2 RED-fix + backend genişletme:
  - **VlanTab CRUD** (`87f1e84`) — Create + Delete + RBAC
  - **PortsTab + BulkVlanAssignDrawer** (`32abaa2`) — row aksiyon + multi-port toplu VLAN ata
  - **BackupTab refactor** (`fee0294`) — Canlı/Yedekler alt-tab + DiffViewerDrawer (react-diff-viewer-continued@^4.2.2 lazy chunk) + drift alert + content preview
  - **TerminalTab hibrit** (`6c184dd`) — yeni 10. sekme: REPL (audit/approval/readonly) + xterm SSH WS
  - **RED retry fix** (`8218451`) — LiveConfig Güvenlik Tarama görünürlük + VLAN trunk Native VLAN ID + Allowed VLANs (parser `_vlanHelper.ts` + 26 unit test)
  - **Backend genişletme + Backup snapshot Güvenlik Tarama** (`a63f5bf`) — `POST /devices/{id}/config/check-policy` opsiyonel `backup_id` body parametresi (DB'den ConfigBackup.config_text çek + offline policy check; eski sözleşme korundu)
- **DB:** Migration **YOK** — `f9aeportpol` korundu.
- **Compose/network/env:** Değişiklik **YOK** — B1c segmentation aynen.
- **Frontend:** Bundle `index-Bim1OQHK.js` → `index-BHE2NjnU.js`; lazy chunk `DiffViewerDrawer` korundu (sadece Diff drawer açılınca indirilir).
- **Backend:** 1 endpoint genişletme (`check-policy` body parametresi); eski sözleşme bozulmadı (body'siz live tarama davranışı aynen).
- **Kesinti:** P4 recreate ~30sn backend blip (504/502 → retry); frontend <1sn. Postgres+redis+nginx+celery×3+event_consumer+beat+flower **DOKUNULMADI** (uptime'lar korundu).

## Final state (POST-DEPLOY ANCHOR)

| | Değer |
|---|---|
| git HEAD | **`f23ca833aaa16429714d374e51153100cee4ef28`** (f23ca83, main) |
| alembic current | `f9aeportpol` (DB migration yok, korundu) |
| Backend image | **`199f1579d85a`** (yeni build, 2026-06-01 08:43 UTC) |
| Frontend image | **`e46f23a8ad95`** (yeni build, 2026-06-01 08:48 UTC) |
| Frontend bundle hash | `index-BHE2NjnU.js` + `index-B7-a2UwE.css` (önceki: `Bim1OQHK.js`) |
| 11/11 servis | Up/healthy (backend+frontend ~1dk, diğerleri 23sa-2gün korundu) |
| Host listening | yalnız `0.0.0.0:80` + `0.0.0.0:443` (B1c korundu) |
| Disk | 47G / 33G kullanılan / 12G boş / %74 (deploy öncesiyle aynı) |
| RLS-forced tablo | **61** (Faz 7 + C7 anchor, korundu) |

### Çalışan image digest'leri (final)

```
netmanager-backend-1                199f1579d85a                          (Wave 1 build — check-policy backup_id support)
netmanager-frontend-1               e46f23a8ad95                          (Wave 1 build — 10 sekme + Backup snapshot scan UI)
netmanager-celery_default_worker-1  7b860886da0c207f9540a9e4143b42f82ef454cf9512d5bf99dce3f97962581a  (dokunulmadı)
netmanager-celery_worker-1          09ef1ae6fec99f547d9a469a8d405d01fb595048a616087cfb5b074ce030c1f3  (dokunulmadı)
netmanager-celery_agent_worker-1    ec9c54caf49367302e634f417fe6f89b96ab15272f21df961fa0488dc1086a0b  (dokunulmadı)
netmanager-celery_beat-1            c7e6e92c3ac9fac6aff67dcc40cb8d8b38506b22cc6dcce59c4c98d9ac9dce6b  (dokunulmadı)
netmanager-event_consumer-1         d1034c2940ce9c36b68ce791e08c202824f5e111b3a8ad465126832c333af1a7  (dokunulmadı)
netmanager-flower-1                 e5616f1b31b8c13a24ad798e432a6140fb1950abb67af6678fcd0c9b6b7613e5  (dokunulmadı)
netmanager-nginx-1                  6769dc3a703c719c1d2756bda113659be28ae16cf0da58dd5fd823d6b9a050ea  (dokunulmadı)
netmanager-postgres-1               fffeb6af732486273a29559a4caefe1b917f0324703c6e1356e2fac3ba267f9d  (dokunulmadı)
netmanager-redis-1                  13b871aaf97fdce1787aef62e24345c07906bdca4f38e947550286d917b737ee  (dokunulmadı)
```

## PRE-DEPLOY ROLLBACK ANCHOR (geri-dönüş için saklı)

| | Değer |
|---|---|
| git | `d1f8e6f6bd8aa747c8cba8c2b489a61ec08c496d` (C7 + post-bugfix son hali) |
| alembic | `f9aeportpol` (aynı — DB migration yok) |
| pg_dump | (Wave 1'de yok — DB migration yok, dump alınmadı; gerekirse C7 deploy dump kullanılabilir: `backups/pre-c7-deploy-20260530_112911.dump`) |
| pre-deploy backend image | `sha256:88b7480009894f9637d26f244c61806f97ce967d4d07c8ae9f6dd26e273cdcb7` (C7 build) |
| pre-deploy frontend image | `sha256:ee492c9bb0f9d25a0934ab91bc18a324dedbdedf7c127cae0bb3af10f62296e4` (post-bugfix build) |

> Rollback: image rollback yeterli (DB değişmedi). `docker tag <eski digest> netmanager-{backend,frontend}:latest` + `docker compose up -d --no-deps backend frontend`.

## P-fazı icra özeti

| P | Adım | Yazma | Sonuç |
|---|---|---|---|
| **P0** | Anchor (git + alembic + 11 image digest) | yok | ✅ Read-only, anchor kaydedildi |
| **P1** | Disk + ingress + network check | yok | ✅ 12G boş; host yalnız 80/443; 10 servis `netmanager_internal` ağında |
| **P2** | `git fetch` + `git merge --ff-only f23ca83` | working tree | ✅ HEAD = `f23ca83`; tracked clean |
| **P3** | `docker compose build backend frontend` | image | ✅ backend `199f1579` + frontend `e46f23a8` yeni digest'ler; pre-existing warning'ler dışında error yok (workbox glob + Rollup antd dynamic+static — bundle etkilemez); çalışan container'lar eski image'da kaldı |
| **P4** | `docker compose up -d --no-deps backend frontend` | servis swap | ✅ BE+FE recreate ~30sn; diğer 9 servis dokunulmadı (postgres/redis/nginx 2gün, celery×3/event_consumer/beat/flower 23h uptime korundu); SSH session bir kez koptu, nohup ile retry başarılı |
| **P5** | Smoke gates | read-only | ✅ Hepsi GREEN (aşağıda) |

## P5 — Smoke gates GREEN ✅

### Backend (creds-less)
```
/health/ready                      200
/login                             200  (SPA)
/devices                           200  (SPA)
/devices/1                         200  (SPA)
/devices/1?tab=terminal            200  (10. sekme deep-link)
/devices/1?tab=backup              200  (BackupTab alt-tab)
```

### Yeni endpoint (backup snapshot policy check)
```
POST /api/v1/devices/1/config/check-policy {"backup_id":1}  →  HTTP 401 "Not authenticated"
```
Bu kanıt: endpoint canlı, body parse'lendi, auth check'ine kadar geldi. 422 (validation error) veya 500 (parse error) olmadı → `backup_id` parametresi backend tarafından doğru tanınıyor.

### DB durumu
- alembic_version = **f9aeportpol** ✅ (DB migration yok, anchor'la aynı)
- RLS forced tablo sayısı = **61** ✅ (Faz 7 + C7 + port_policy_assignments korundu)

### Backend log
- Son 2dk gerçek error YOK (traceback/fatal/5xx — sadece INFO request logları)

### Frontend bundle teyit
- `index-BHE2NjnU.js` (yeni, önceki Bim1OQHK)
- `index-B7-a2UwE.css` (yeni — TerminalTab dark theme + DiffViewerDrawer)

## Doğrulama matrisi

| Özellik | Backend | Frontend | Smoke |
|---|---|---|---|
| VlanTab CRUD (create+delete) | mevcut endpoint | yeni UI | gate P5.1 (`?tab=vlan` 200) |
| PortsTab VLAN row + Bulk drawer | mevcut endpoint (trunk vlan_id list destekli) | yeni component | gate P5.1 |
| BackupTab Canlı/Yedekler alt-tab + Diff drawer + drift alert + content preview | mevcut endpoint'ler | yeni refactor + lazy chunk | gate P5.1 |
| TerminalTab REPL + xterm hibrit | mevcut endpoint'ler | yeni component (10. sekme) | gate P5.1 (`?tab=terminal` 200) |
| LiveConfig Güvenlik Tarama (canlı) | mevcut endpoint | mevcut UI (RED-fix: disabled koşulu gevşetildi) | gate P5.2 (auth wall doğrulandı) |
| **Backup snapshot Güvenlik Tarama** | **yeni body parametresi** | yeni Drawer aksiyonu + Modal | **gate P5.2 (backup_id 401 ✅)** |
| VLAN trunk Native + Allowed VLANs (parser) | mevcut endpoint (vlan_id list) | yeni form + `_vlanHelper.ts` parser | gate P5.1 |

## Riskler & kabul edilenler

- **Backend dokunma kuralının esnetilmesi** — kullanıcı (A) seçeneği ile onayladı (Backup snapshot tarama için 30 LOC backend genişletme). Eski sözleşme korundu (body'siz live tarama davranışı aynen) → backward-compatible.
- **react-diff-viewer-continued ~30KB gzip** lazy chunk olarak korundu, ana bundle'a sızmadı.
- **Pre-existing build warning'leri** (workbox glob + Rollup antd dynamic+static import) — Wave 1 kaynaklı değil; bundle etkilemiyor.
- **SSH session P4 sırasında koptu** — nohup + background retry ile başarıyla tamamlandı; SSH wrap'i için future deploy'larda `-o ServerAliveInterval=10` eklenir.
- **DB migration yok** — pg_dump alınmadı (kapsam dışı); rollback için image-only yeterli.

## Follow-up listesi (Wave 1 sonrası)

| # | Konu | Öncelik | Notlar |
|---|---|---|---|
| 1 | **Cloudflare `/poe` banner soruşturması** | Yüksek | C7 deploy'undan kalan follow-up. |
| 2 | **`/Netmanager/` mockup UI dokunuşları** (Dalga 1.1 veya Dalga 2) | Orta | Kullanıcı açıkça Wave 1'in dışında bıraktı — "fonksiyonel parity tamamlanmadan kozmetik değişiklik istemiyorum". 6 somut dokunuş aday listesi `reference-netmanager-mockup` memory'de. |
| 3 | **P3 — Komşular/Audit/SNMP CPU-RAM sekmeleri** | Orta | Parity matrisinde Dalga 2 olarak listelendi. |
| 4 | **P4 — SNMP Configure modal, Trap Forwarding, per-port UtilizationChart, Visual SwitchPortPanel** | Düşük | Karar bekleyenler — kasıtlı silinmiş mi belirsiz; kullanıcı kararına bağlı. |
| 5 | **`DeviceDetail.tsx` dead code temizliği** | Düşük | Wave 1 referans olarak kullanıldı; artık silinebilir. |
| 6 | **`docker-compose.yml` `version` özelliği obsolete uyarısı** | Çok düşük | Cosmetic. |

## Commit kronolojisi (Wave 1)

```
f23ca83  Merge branch 't10/c7-feature-restore-wave1' — Dalga 1 Feature Restore (8 commit, BE+FE)
a63f5bf  feat(devices/backup): backup snapshot üzerinde Güvenlik Tarama (offline policy check)
8218451  fix(devices/detail): browser smoke RED — Güvenlik Tarama görünürlüğü + VLAN trunk Allowed VLANs
6c184dd  feat(devices/detail): TerminalTab — REPL + Canlı SSH hibrit (Dalga 1 commit 5/5)
fee0294  feat(devices/detail): BackupTab refactor — Canlı/Yedekler alt-tab + Diff (Dalga 1 commit 4/5)
32abaa2  feat(devices/detail): PortsTab VLAN Ata — row + bulk (Dalga 1 commit 3/5)
87f1e84  feat(devices/detail): VlanTab CRUD — create + delete (Dalga 1 commit 2/5)
0fe26ea  chore(frontend): add react-diff-viewer-continued@^4.2.2
06b7c91  docs(c7): T10_C7_FEATURE_PARITY.md — modal→sayfa parity envanteri (Dalga 1 başlangıç ref)
```

## Rollback prosedürü (geri dönüş gerekirse)

**Tetikleyiciler:** Backend 5xx sürekli / SPA route'lar fail / Backup snapshot tarama 500 dönüyor / Terminal sekmesi crash / RLS izolasyon kırıldı.

**Prosedür (image rollback — DB değişmediği için yeterli):**
1. Yazan servisleri durdur: `docker compose stop backend frontend`
2. Eski tag'leri ata:
   ```
   docker tag sha256:88b7480009894f… netmanager-backend:latest
   docker tag sha256:ee492c9bb0f9… netmanager-frontend:latest
   ```
3. `docker compose up -d --no-deps backend frontend`
4. Smoke gate 1-2 yeşil + alembic `f9aeportpol` teyit (zaten değişmedi)
5. Git rollback opsiyonel: `git checkout d1f8e6f` (sonraki deploy için)

**Yedek plan:** `f9aeportpol` migration aynen kaldığı için DB rollback gerekmez. Backend image rollback'i tek başına yeterli — eski endpoint sözleşmesi de korunmuş.

---

## P-sonu Status

**DEPLOY COMPLETED ✅** — Wave 1 (Feature Restore) canlıda fonksiyonel; modal→sayfa parity kapandı; backup snapshot offline policy tarama aktif; backend genişletme backward-compatible; tüm 11 container healthy; rollback anchor + image digest'leri kayıtlı.

Sıradaki çalışma: **Cloudflare `/poe` banner soruşturması** (C7 deploy'undan kalan follow-up #1).
